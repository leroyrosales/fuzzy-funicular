{% extends "base.twig" %}

{% block content %}

	<div class="container">
		<div class="content-wrapper">
			<div class="row justify-content-center">
				<div class="col-12 my-3 py-3 text-center">
					<a href="{{ page_for_posts_link }}" class="text-secondary text-decoration-none h5">&larr; Back to {{ page_for_posts_title }}</a>
				</div>
				<div class="col-12 col-md-6 mt-1 pt-1 pb-3">
					<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
						<section class="article-content">
							<h1 class="article-h1 text-center">{{ post.title }}</h1>
							<img class="img-fluid w-100 mb-4" src="{{ post.thumbnail.src }}">
							<div class="article-body">
								{{post.content}}
							</div>
						</section>
					</article>
				</div>
			</div>
		</div><!-- /content-wrapper -->
	</div>

{% endblock %}
